# Kutuliza – Python backend dependencies
# Install with: pip install -r requirements.txt

fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9   # multipart form/file upload support
httpx==0.27.0              # async-ready HTTP client for Sunbird API
python-dotenv==1.0.1       # .env file loading
mutagen==1.47.0            # audio duration validation
pydantic==2.7.1            # data validation (bundled with FastAPI)
